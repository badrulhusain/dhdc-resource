# Environment (development, production)
NODE_ENV=production

# MongoDB Atlas Connection String
# Get this from https://www.mongodb.com/cloud/atlas
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dhdc?retryWrites=true&w=majority

# JWT Secret for token signing (MUST CHANGE IN PRODUCTION!)
# Generate a secure random string: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-change-in-production

# Iframely API Configuration
# Get your API key from https://iframely.com/
IFRAME_API_KEY=your-iframely-api-key
IFRAME_END_POINT=https://iframe.ly/api/iframely?url={encoded_url}&api_key=your-iframely-api-key

# Optional: Ping message for health checks
PING_MESSAGE=pong

# Optional: Builder.io (if using)
VITE_PUBLIC_BUILDER_KEY=your-builder-key

# Google Drive Service Account (for GDRIVE_FOLDER support)
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
